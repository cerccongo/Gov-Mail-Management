import os

ALLOWED_HOSTS = ["*"]  # Pour le test, à restreindre en production

STATIC_ROOT = os.path.join(BASE_DIR, "staticfiles")

# Pour servir les fichiers statiques avec WhiteNoise
MIDDLEWARE = [
    "whitenoise.middleware.WhiteNoiseMiddleware",
] + MIDDLEWARE
